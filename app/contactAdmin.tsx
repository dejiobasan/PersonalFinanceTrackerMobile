import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const ContactAdmin = () => {
  return (
    <View style={styles.container}>
      <Text>contactAdmin</Text>
    </View>
  )
}

export default ContactAdmin

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  }
})